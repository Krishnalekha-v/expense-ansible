- name: mysql coniguration
  hosts: mysql
  task:
  - name: install mysql
    ansible.builtin.package:
    name: mysql-server
    state: present
  - name: start mysql
    ansible.builtin.service:
    name: mysqld
    state: started
    enabled: yes
